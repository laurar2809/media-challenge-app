<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' · ' : '' %>CRUD Liste</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <nav class="navbar navbar-expand-lg bg-primary navbar-dark"> <!--Navigationsleiste-->
    <div class="container">
      <a class="navbar-brand fw-semibold" href="/">Multimedia Challenge</a>
        <div class="collapse navbar-collapse" id="navbarNav"> <!-- Leiste linksbündig, neben CRUD LISTE -->
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link <%= activePage === 'kategorien' ? 'active' : '' %>" 
            aria-current="<%= activePage === 'kategorien' ? 'page' : '' %>"
            href="/">Kategorien </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= activePage === 'challenges' ? 'active' : '' %>" 
            aria-current="<%= activePage === 'challenges' ? 'page' : '' %>"
            href="/challenges">Challenges </a>
            </li>
          </ul>
            <div class="d-flex"> <!-- Button rechts-->
              <a href="/api/items" class="btn btn-outline-light btn-sm">API JSON</a>
            </div>
        </div>
      </div>
  </nav>

  <main class="container my-4">
    <% if (flash && flash.error && flash.error.length) { %>
      <div class="alert alert-danger"><%= flash.error[0] %></div>
    <% } %>
    <% if (flash && flash.success && flash.success.length) { %>
      <div class="alert alert-success"><%= flash.success[0] %></div>
    <% } %>

    <%- body %>
  </main>

  <footer class="border-top py-3 small text-muted">
    <div class="container">
    Beispiel-App • Node.js + Express + Knex (SQLite/PostgreSQL/MySQL) + Bootstrap • REST API & Uploads
      </div>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
