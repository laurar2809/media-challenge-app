<!DOCTYPE html>
<html lang="de">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>
    <%= typeof pageTitle !=='undefined' ? pageTitle + ' · ' : '' %>CRUD Liste
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/style.css">
</head>

<body >

  <% // SICHERHEITS-FALLBACK
   if (typeof currentUser === 'undefined') { 
     currentUser = null; 
   } 
   %>

  <% // FALLBACK FÜR activePage if (typeof activePage==='undefined' ) { activePage='' ; } %>
    <nav class="navbar navbar-expand-lg bg-primary navbar-dark navbar sticky-top"> <!--Navigationsleiste-->
      <div class="container">
        <a class="navbar-brand fw-semibold" href="/">Multimedia Challenge</a>

        <!-- BURGER MENU TOGGLER FÜR MOBILE -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>



        <div class="collapse navbar-collapse" id="navbarNav"> <!-- Leiste linksbündig, neben CRUD LISTE -->
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <!--für alle sichtbar!!!:-->
            <li class="nav-item">
              <a class="nav-link <%= activePage === 'kategorien' ? 'active' : '' %>"
                aria-current="<%= activePage === 'kategorien' ? 'page' : '' %>" href="/">Kategorien </a>
            </li>
             <% if (currentUser && currentUser.user_role_id !== 1) { %>
            <li class="nav-item">
              <a class="nav-link <%= activePage === 'aufgabenpakete' ? 'active' : '' %>" 
                href="/aufgabenpakete">Aufgabenpakete</a>
            </li>
            <% } %>
            <li class="nav-item">
              <a class="nav-link <%= activePage === 'challenges' ? 'active' : '' %>" 
                aria-current="<%= activePage === 'challenges' ? 'page' : '' %>"
                href="/challenges">Challenges</a>
            </li>
            <!--Nur für Lehrer und Admin sichtbar!:-->
             <% if (currentUser && currentUser.user_role_id !== 1) { %>
            <li class="nav-item">
              <a class="nav-link <%= activePage === 'schueler' ? 'active' : '' %>"
                aria-current="<%= activePage === 'schueler' ? 'page' : '' %>" 
                href="/schueler">Schüler</a>
            </li>
            <li class="nav-item">
            <a class="nav-link <%= activePage === 'lehrer' ? 'active' : '' %>"
                aria-current="<%= activePage === 'lehrer' ? 'page' : '' %>" 
                href="/lehrer">Lehrer</a>
            </li>
             <% } %>
          </ul>
          <!-- RECHTS: User Info & Login/Logout -->
          <div class="navbar-nav align-items-center">
            <% if (currentUser) { %>
              <!-- EINGELOGGT: User Info + Logout -->
              <span class="navbar-text me-3">
                 <%= currentUser.vorname %> 
                <small class="text">(<%= currentUser.rolle %>)</small>
              </span>
              <form action="/auth/logout" method="POST" class="d-inline">
                <button type="submit" class="btn btn-outline-light btn-sm" 
                        onclick="console.log('Logout button clicked')">
                  Logout
                </button>
              </form>
            <% } else { %>
              <!-- NICHT EINGELOGGT: Login Button -->
              <a href="/auth/login" class="btn btn-outline-light btn-sm">Login</a>
            <% } %>
          <div class="d-flex"> <!-- Button rechts-->
            <a href="/api/categories" class="btn btn-outline-light btn-sm">API JSON</a>
          </div>
        </div>
      </div>
    </nav>

    <main class="container my-4">
      <% if (flash && flash.error && flash.error.length) { %>
        <div class="alert alert-danger">
          <%= flash.error[0] %>
        </div>
        <% } %>
          <% if (flash && flash.success && flash.success.length) { %>
            <div class="alert alert-success">
              <%= flash.success[0] %>
            </div>
            <% } %>

              <%- body %>
    </main>

    <footer class="border-top py-3 small text-muted">
      <div class="container">
        Beispiel-App • Node.js + Express + Knex (SQLite/PostgreSQL/MySQL) + Bootstrap • REST API & Uploads

        <% if (currentUser) { %>
          Eingeloggt als: <%= currentUser.rolle %>
        <% } else { %>
          Nicht eingeloggt
        <% } %>

      </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>