<% pageTitle="Login" %>

<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-4">
            <div class="card mt-5">
                <div class="card-header text-center">
                    <h4 class="mb-0">🔐 Einfacher Login</h4>
                </div>
                <div class="card-body">
                    <div class="alert alert-info text-center">
                        <strong>Einfache Passwörter:</strong><br>
                        <code>1</code> = Schüler<br>
                        <code>2</code> = Lehrer<br> 
                        <code>3</code> = Admin
                    </div>

                    <form action="/auth/login" method="POST">
                        <div class="mb-3">
                            <label class="form-label">Rolle eingeben (1, 2 oder 3)</label>
                            <input type="password" class="form-control text-center" name="password" 
                                   placeholder="1, 2 oder 3" required autofocus>
                        </div>
                        
                        <button type="submit" class="btn btn-primary w-100">Anmelden</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>