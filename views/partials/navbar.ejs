<nav class="navbar main-navbar">
  <div class="container position-relative d-flex align-items-center">


    </svg> <span class="brand-text">Multimedia Challenge</span> </a>

    <!-- MITTE: Navigation (ohne Burger, immer sichtbar) -->
    <div class="navbar-center">
      <ul class="navbar-nav navbar-nav-center">
        <% if (currentUser && currentUser.user_role_id !==1) { %>
          <li class="nav-item"> <a class="nav-link <%= activePage === 'kategorien' ? 'active' : '' %>"
              href="/">Kategorien</a> </li>
          <li class="nav-item"> <a class="nav-link <%= activePage === 'aufgabenpakete' ? 'active' : '' %>"
              href="/aufgabenpakete">Aufgabenpakete</a> </li>
          <% } %>

            <li class="nav-item"> <a class="nav-link <%= activePage === 'challenges' ? 'active' : '' %>"
                href="/challenges">Challenges</a> </li>
                
            <% if (currentUser && currentUser.user_role_id ==1) { %>
            <li class="nav-item">
              <a class="nav-link <%= activePage === 'schueler-abgaben' ? 'active' : '' %>" href="/schueler/abgaben">
                Abgaben
              </a>
            </li>
            <%}%>

            <% if (currentUser && currentUser.user_role_id !==1) { %>
              <li class="nav-item"> <a class="nav-link <%= activePage === 'schueler' ? 'active' : '' %>"
                  href="/schueler">Schüler</a> </li>
              <li class="nav-item"> <a class="nav-link <%= activePage === 'lehrer' ? 'active' : '' %>"
                  href="/lehrer">Lehrer</a> </li>
              <li class="nav-item"> <a class="nav-link <%= activePage === 'teams' ? 'active' : '' %>"
                  href="/teams">Teams</a> </li>
              <li class="nav-item"> <a class="nav-link <%= activePage === 'bewertung' ? 'active' : '' %>"
                  href="/bewertung">Abgaben</a> </li>
              <% } %>
      </ul>
    </div> <!-- RECHTS: User + Logout/Login -->
    <div class="navbar-user-area">
      <% if (currentUser) { %> <span class="navbar-text user-info"> <i class="bi bi-person"></i>
          <%= currentUser.vorname %>
            <%= currentUser.nachname %> <small class="user-role">(<%= currentUser.rolle %>)</small>
        </span>
        <form action="/auth/logout" method="POST"> <button type="submit" class="btn btn-outline-light btn-sm">
            Logout <i class="bi bi-box-arrow-right ms-2"></i> </button> </form>
        <% } else { %> <a href="/auth/login" class="btn btn-outline-light btn-sm"> Login <i
              class="bi bi-box-arrow-in-right ms-2"></i> </a>
          <% } %>
    </div>
  </div>
</nav>